{
  "name": "spectre-dw-spacetimedb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "spacetime build --module-path .",
    "generate": "spacetime generate --lang typescript --out-dir ../src/module_bindings --module-path . --yes"
  },
  "dependencies": {
    "spacetimedb": "^2.4.1"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}
